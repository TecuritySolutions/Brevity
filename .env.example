NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Gmail SMTP — use an App Password, not your regular Gmail password
# Enable 2FA → Google Account > Security > App Passwords → generate one
GMAIL_USER=cto.tecurity@gmail.com
GMAIL_APP_PASSWORD=gpbq bpml hqjw cswa

# The public URL of your app — used to build the verification link in emails
NEXT_PUBLIC_APP_URL=http://localhost:3000
